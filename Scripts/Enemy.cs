using UnityEngine;
using System.Collections;

public class Enemy : MonoBehaviour {

	//Enemy will detect near enemies and move to their latest location in order to attack
	//It can also attack buildings, it looks for player units and buildings until none is left

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
